{
  "name": "kittyhawk-template",
  "version": "1.0.1",
  "main": "main.js",
  "types": "main.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "import": "yarn run cdk8s import",
    "compile": "tsc",
    "watch": "tsc -w",
    "build": "yarn run compile && yarn run cdk8s synth",
    "upgrade": "yarn add cdk8s@latest && yarn add cdk8s-cli@latest -D"
  },
  "dependencies": {
    "@pennlabs/kittyhawk": "^1.0.5",
    "cdk8s": "^1.0.0-beta.3",
    "constructs": "^3.2.64"
  },
  "devDependencies": {
    "@types/node": "^14.14.11",
    "cdk8s-cli": "^1.0.0-beta.3",
    "typescript": "^4.1.2"
  }
}
